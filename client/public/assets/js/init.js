(function($) {
   $(function() {
      $(".parallax").parallax();
      $(".sidenav").sidenav();

      $(".dropdown-trigger").dropdown();

      
      $('.modal').modal();
      $('#addcontact').openModal();

      $(".parallax").parallax();
   }); // end of document ready
})(jQuery); // end of jQuery name space
